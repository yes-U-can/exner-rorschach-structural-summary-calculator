# [2026-07-16] v2.2.2 Cn 계산 경계 핫픽스

## 주요 수정사항

### 개요

v2.2.2는 Cn이 들어가는 계산과 들어가지 않는 계산을 분리하고, 형태질(FQ)이 비어 있는 미완성 행의 GHR/PHR 분류를 바로잡은 핫픽스입니다.

먼저 중요한 점이 하나 있습니다. v2.2.1의 `FC:CF+C` 오른쪽 값은 이미 Cn을 포함해 올바르게 계산됐습니다. **완성된 프로토콜은 이 이유만으로 다시 계산할 필요가 없습니다.** 이번 패치에서는 Cn이 들어가는 화면 비율과 Cn이 들어가지 않는 WSumC, S-CON 7번 기준, Color-Shading 계산을 서로 분리했습니다. 형태질(FQ)을 아직 입력하지 않은 미완성 행이 GHR 또는 PHR로 임시 분류되던 문제도 함께 바로잡았습니다.

계산 범위는 이 앱이 실제로 보여주는 Exner Comprehensive System 구조요약입니다. 다른 로샤 체계, 연구용 확장 지표와 임상 진단 자동화는 포함하지 않습니다.

### 세부사항

이번 패치에서는 Cn 포함 여부가 서로 다른 계산을 각각의 기준에 맞게 적용했습니다.

| 사용 위치 | v2.2.2에서 고정한 계산 |
| --- | --- |
| 화면의 관례적 라벨 `FC:CF+C` | `FC : (CF + C + Cn)` |
| WSumC | `0.5*FC + 1.0*CF + 1.5*C` |
| S-CON criterion 7 | `CF + C > FC` |
| Color-Shading blend의 chromatic color | `FC`, `CF`, `C`만 포함 |

Cn은 첫 번째 화면 비율에는 들어가지만 나머지 세 계산에는 들어가지 않습니다. 비슷해 보이는 색채 합계를 하나로 묶어 쓰지 않고, 각 계산이 필요한 부호만 따로 합산하도록 정리했습니다. AI 도우미도 같은 기준으로 설명합니다.

## 이번 릴리즈에서 바뀐 것과 바뀌지 않은 것

### 바뀐 것

- 형태질(FQ)이 비어 있는 미완성 행은 더 이상 GHR 또는 PHR로 잠정 분류하지 않습니다. FQ를 실제로 입력한 뒤에만 7단계 판정 순서를 적용합니다.
- 화면의 Cn 포함 값과 S-CON의 Cn 제외 값을 서로 다른 계산으로 분리해, 한쪽의 변경이 다른 쪽에 섞이지 않도록 했습니다.
- 다섯 언어의 `FC:CF+C`와 Cn 참조 문서, AI 도우미가 검색하고 설명하는 내용도 같은 계산 범위를 구분합니다.
- 코딩 도우미와 해석 도우미가 Cn의 서로 다른 계산 경계를 구분하고 `2:5` 같은 완성된 결과를 설명하도록 보강했습니다.
- 좌측 패널의 반투명 효과를 제거하고 완전 불투명 배경으로 고정했습니다.

### 바뀌지 않은 것

- v2.2.1에서 이미 보여주던 Cn 포함 화면값은 유지했습니다.
- 계산 화면에 연령 입력 UI를 추가하지 않았습니다.
- API 키 없이 사용하는 계산기 본체의 개인정보 최소수집 원칙을 바꾸지 않았습니다.
- AI가 부호화나 임상 해석을 자동 확정하도록 범위를 넓히지 않았습니다.

## 계산 기준과 참고 자료

앱의 계산 기준을 설명할 때 다음 두 공개 자료를 참고합니다.

1. 공개 배포된 2019 Excel scoring workbook의 셀 수식
2. Jeremy Leader의 공개 프로젝트 [RorScore](https://github.com/jleader/RorScore)

2019 workbook의 공개 배포 위치는 [Naver Blog 게시물](https://blog.naver.com/jin_k84/221539279596)이며, workbook 안에서 확인되는 제작 표기는 `[Scoring Program] _by. Ju-Ri`입니다. 이 표기만으로 실명을 추정하지 않고 원본 파일도 재배포하지 않습니다.

어느 자료 하나만을 정답으로 삼지 않습니다. 이 앱이 채택한 규칙, 완성된 구조요약 예시와 서로 다른 프로그램의 출력을 함께 참고하며, GPT-5.5 답변은 계산 규칙의 근거로 사용하지 않습니다.

## 다섯 질문에 대한 답

### 1. 원본 Excel이 잘못되었는가?

**일부는 맞고 일부는 달랐습니다.**

맞았던 대표 항목:

- `WSumC = 0.5*FC + CF + 1.5*C`
- EA, es, AdjEs의 기본 합산
- 자기중심성 지수에서 반사반응을 3배 가중하는 구조
- 화면의 관례적 `FC:CF+C` 오른쪽 값에 Cn을 포함하는 셀 수식

앱이 선택한 규칙과 달랐던 대표 항목:

- D/AdjD를 `-5`에서 `+5`까지만 반환
- EB 양쪽이 양수이면 정해진 표시 조건을 충족하지 않아도 EBPer를 표시
- WDA%와 Afr의 분모가 0일 때 Excel 오류 발생
- S-CON과 Color-Shading에서 Cn 포함 색채 합계를 재사용

따라서 “Excel 전체가 틀렸다”도 아니고 “Excel과 같으니 앱도 맞다”도 아닙니다. 셀마다 목적과 규칙을 다시 분리해야 했습니다.

### 2. Excel의 차이는 임상적으로 얼마나 특수한 경우에 나타나는가?

공개 발생률 자료가 없으므로 임의의 퍼센트를 만들지 않고 **정확한 발동 조건**으로 설명합니다.

| 항목 | 발동 조건 | 범위 판단 |
| --- | --- | --- |
| D/AdjD 상한 | `EA-es` 또는 `EA-AdjEs`가 `+15.0`보다 크거나 `-15.0`보다 작음 | 큰 자원-부담 불균형이 필요한 극단 구간 |
| EBPer 과잉 표시 | EB 양쪽이 양수지만 EA, Lambda 또는 EA 구간별 절대 차이 조건을 충족하지 않음 | 희귀 부호 없이도 가능한 비교적 넓은 범위 |
| WDA% 0분모 | W, WS, D, DS 반응이 전혀 없음 | 표준 실시에서는 매우 이례적 |
| Afr 0분모 | I-VII 카드 반응이 전혀 없음 | 표준 카드 실시라면 불완전 입력에 가까움 |
| S-CON의 Cn 오염 | Cn이 있고 `CF+C`와 FC가 criterion 경계에 가까움 | Cn 자체가 드물지만 1점 경계를 바꿀 수 있음 |
| Color-Shading의 Cn 오염 | Cn과 shading 결정인이 같은 blend 문맥에 걸림 | 매우 특수한 조합 |

D/AdjD 예시: 프로토콜 전체 합이 `EA=3.5`, `es=19.0`이면 원차는 `-15.5`입니다. 상한형 수식은 `D=-5`에서 멈추지만, 연속되는 2.5 간격 변환은 `D=-6`입니다. “어두운 구름이 바람에 밀려 내려오고 있어요” 같은 가상 응답에서 Inquiry로 무생물 운동과 diffuse shading이 확인되면 `m`, `Y` 계열 후보가 될 수 있습니다. 다만 문장 하나로 부호나 D 값을 확정하지 않으며, 여러 반응의 누적 합이 발동 조건을 만듭니다.

EBPer 예시: `M=7`, `WSumC=4`, `EA=11`, `Lambda=.55`이면 양쪽 차이가 3이므로 이 EA 구간의 표시 조건을 충족하고 EBPer는 `1.75`입니다. 단순히 비율이 2.5 이상인지 보는 방식은 이 값을 잘못 숨깁니다.

### 3. Excel을 바탕으로 만든 앱도 잘못되었는가?

**v1과 v2.2.0에는 실제 결함이 있었습니다.** 다만 모든 결함이 Excel에서 그대로 복사된 것은 아닙니다.

| 항목 | 2019 Excel | v1 GAS | v2.2.0 | v2.2.1 | v2.2.2 |
| --- | --- | --- | --- | --- | --- |
| D/AdjD | `+/-5` 상한 | 상한 계승 | 상한 계승 | 상한 제거 | 상한 없음 유지 |
| EBPer | 단순 비율 | 단순 비율 계승 | 다른 잘못된 표시 조건 | 정해진 표시 조건 | 같은 조건 유지 |
| GHR/PHR | 셀 기반 판정 | 순서 일부 축약 | 축약 계승 | 7단계 순서 | 7단계 순서·빈 FQ 보완 |
| 화면 `FC:CF+C` | Cn 포함 | Cn 누락 | Cn 누락 | Cn 포함 | Cn 포함 유지·적용 범위 분리 |
| WSumC | Cn 제외 | Cn 제외 | Cn 제외 | Cn 제외 | Cn 제외 유지 |
| S-CON c7 | Cn 포함 합계 사용 | Cn 제외 | Cn 제외 | Cn 제외 | Cn 제외 기준 유지 |
| Color-Shading | Cn 포함 범위 사용 | Cn 제외 | Cn 제외 | Cn 제외 | FC·CF·C만 포함 |
| WDA%/Afr 0분모 | 오류 | 0으로 대체 | 0으로 대체 | 정의 불가 `-` | 정의 불가 `-` 유지 |
| 자기중심성 지수 | 반사반응 x3 | x2로 잘못 옮김 | v2.2.0 이전 x3 교정 | x3 유지 | x3 유지 |
| ZEst | 표 기반 | 마지막 경계 오류 | 교정 이력 | 유효한 정수만 계산 | 전체 표 적용 |

v2.2.1의 계산기는 Cn 화면값을 이미 올바르게 포함했습니다. v2.2.2에서는 화면 비율, WSumC, S-CON, Color-Shading이 서로 다른 색채 합계를 사용하도록 분리했습니다.

### 4. 앱 결함은 임상적으로 얼마나 특수한 경우에 나타났는가?

- **D/AdjD 상한:** `+/-15`를 벗어나는 좁고 극단적인 구간에서 심한 정도를 한 단계 이상 축소할 수 있었습니다.
- **EBPer:** 이 항목들 가운데 적용 범위가 가장 넓었습니다. 희귀 부호가 없어도 EA, Lambda, 양쪽 절대 차이의 조합에 따라 표시 여부가 달라질 수 있었습니다.
- **GHR/PHR:** 좋은 FQ의 인간표상에 `ALOG`, `CONTAM` 또는 Level 2 인지 특수점수가 함께 있는 것처럼 흔하지 않지만 가능한 조합에서 우선순위가 달라질 수 있었습니다.
- **v1·v2.2.0의 Cn 화면 누락:** Cn이 한 개 이상인 비교적 좁은 범위에서 Lower Section 비율 오른쪽 값이 작게 표시됐습니다.
- **WDA%/Afr 0분모:** 표준 실시에서는 매우 이례적이지만, 정의할 수 없는 값을 정상적인 0으로 보이지 않는 것이 중요했습니다.

GHR/PHR 기전 예시: “두 사람이 서로 밀고 있어요. 둘이 떨어져 있으니 같은 사람인 게 분명해요.”라는 가상 응답에서 영역·형태·운동이 확인되고 마지막 설명이 ALOG로 판정된다면 `Do Ma H ALOG` 같은 후보가 될 수 있습니다. 좋은 FQ의 인간표상이어도 ALOG가 있으면 7단계 중 앞선 PHR 조건이 적용됩니다. 실제 부호화는 반응기록과 질문단계(Inquiry)를 확인한 인간 채점자가 수행합니다.

Cn 기전 예시: “이건 빨간색이에요. 모양은 모르겠고 그냥 빨간색입니다.”가 순수한 색 이름 반응으로 확인되면 `Cn`, FQ none 후보입니다. `FC=2`, `CF=4`, `C=0`, `Cn=1`이라면:

- 화면 `FC:CF+C` 결과는 `2:5`
- WSumC는 `0.5*2 + 4 + 1.5*0 = 5.0`
- S-CON c7은 `4 > 2`를 판정하며 화면 우변 5를 재사용하지 않음
- Color-Shading의 chromatic color 범위에도 Cn을 넣지 않음

### 5. 무엇을 기준으로 맞고 틀림을 판단했는가?

1. 앱이 채택한 Exner Comprehensive System 판본의 명시 계산식과 순서 규칙
2. 같은 범위의 완성 구조요약 예제와 출력
3. RAP3, RIAP5, RorScore처럼 별도로 만들어진 프로그램의 재현 가능한 출력과 CHESSSS 공개 설명서
4. 2019 Excel과 앱 버전별 계산 결과

공개 프로그램끼리 결과가 같다는 이유만으로 규칙을 확정하지 않았습니다. 특히 S-CON 7번과 Color-Shading에서 Cn을 다루는 방식은 공개 자료의 설명이 부족하거나 서로 달랐으므로, 모든 프로그램에 통용되는 유일한 공식이라고 주장하지 않습니다. 이 문서는 현재 앱이 채택한 판본과 구조요약 범위의 계산 기준을 설명합니다. GPT-5.5의 답변도 계산 정답으로 사용하지 않았습니다.

## 다섯 언어 예시

계산기는 자연어 메모가 아니라 입력된 부호를 계산합니다. 다음 문장들은 문화권별 규준을 주장하기 위한 사례가 아니라, 같은 Cn 개념을 다섯 언어에서 설명하기 위한 **공통 가상 예시**입니다.

| 언어 | 가상 응답 | 가능한 후보 |
| --- | --- | --- |
| 한국어 | 이건 빨간색이에요. 모양은 모르겠고 그냥 빨간색입니다. | `Cn`, FQ none |
| English | It is red. I do not see a shape; it is just the color red. | `Cn`, FQ none |
| 日本語 | 赤い色です。形は分からず、ただ赤い色に見えます。 | `Cn`, FQ none |
| Español | Es de color rojo. No veo una forma; solo es el color rojo. | `Cn`, FQ none |
| Português | É vermelho. Não vejo uma forma; é apenas a cor vermelha. | `Cn`, FQ none |

실제 Cn 판정에는 반응기록과 질문단계(Inquiry)가 필요합니다. 번역문만으로 부호를 자동 확정하지 않습니다.

## 참조 문서와 AI 도우미

API 키 없이 사용하는 계산기는 AI와 무관하게 같은 계산식을 사용합니다. 선택 기능인 AI 도우미와 다섯 언어 참조 문서도 Cn의 서로 다른 계산 범위를 구분해 설명합니다.

AI 도우미의 설명은 모든 실제 대화의 임상적 정확성을 보증하지 않습니다.

## 판단 기준과 남아 있는 한계

계산식은 이 앱이 채택한 명시 규칙, 완성된 구조요약 예시와 실제 프로그램 출력을 함께 참고했습니다. 어느 한 프로그램이나 AI 답변도 단독 정답으로 사용하지 않았습니다.

계산기는 입력된 부호를 집계하며 잘못 부호화된 입력을 임상적으로 교정하지 않습니다. CHESSSS 공개 설명서에서 직접 확인할 수 없는 수식 주장은 근거에서 제외했습니다. 최종 부호화와 해석은 자격을 갖춘 전문가의 판단 영역입니다.

## 공개 참고 자료

- [Rorschach Workshops: A Rorschach Workbook for the Comprehensive System, 5th ed. 서지](https://rorschachworkshops.com/product/a-rorschach-workbook-for-the-comprehensive-system-5th-ed/)
- [International Rorschach Institute: CHESSSS 1 배포 페이지](https://www.rorschach-institute.org/download%20_chessss1.html)
- [RorScore 원본 저장소](https://github.com/jleader/RorScore)
- [RAP3 Structural Summary report](https://virtualpsychology.com/documentation/rap_plus/Structural%20Summary%20report%20-%20Virtual%20Psychology.pdf)
- [RIAP5 공개 sample report](https://hogrefe.se/userfiles/files/RIAP5IR_SAMPLE.pdf)
- [2019 Excel workbook 공개 배포 게시물](https://blog.naver.com/jin_k84/221539279596)

RAP3와 RIAP5의 공개 출력, RorScore는 다른 프로그램의 같은 항목을 비교하기 위한 자료입니다. CHESSSS 링크는 공개 설명서와 배포 정보이며, 이 자료들을 Exner 원전의 대체물이나 임상적 권위의 인증으로 제시하지 않습니다.
